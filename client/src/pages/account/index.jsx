import React from 'react';
import Profile from './Profile';

// Page par défaut pour /account qui redirige vers le profil
const AccountIndex = () => {
  return <Profile />;
};

export default AccountIndex;
